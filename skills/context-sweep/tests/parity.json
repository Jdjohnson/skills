{
  "skill": "context-sweep",
  "parity_goal": "Preserve deterministic daily-note writes and per-source checkpoints while making all source providers configurable.",
  "cases": [
    {
      "id": "normal-run",
      "prompt": "$context-sweep",
      "expected": ["configured sources", "deduplicated log entries", "successful checkpoints advance"]
    },
    {
      "id": "dry-run",
      "prompt": "$context-sweep dry-run",
      "expected": ["no file mutation", "no checkpoint advancement"]
    },
    {
      "id": "since-override",
      "prompt": "$context-sweep since:2026-04-22T09:00:00-05:00",
      "expected": ["override lower bound", "forward-only checkpoints", "honest source limits"]
    }
  ]
}
