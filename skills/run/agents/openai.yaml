interface:
  display_name: "Run"
  short_description: "Plan, harden, and supervise multi-step project sessions"
  default_prompt: "Use $run to plan a substantial project or hardening run, then hand back a launch-ready package."
policy:
  allow_implicit_invocation: true
